자바스크립트

자료형
숫자,문자, 불린, undefined
== ,!=
===, !== 의 관계 

변수
var 키워드를 이용하여 변수를 선언할 수 있다

변수명
$,_를 제외한 특수문자는 변수명으로 못쓴다.
숫자로 시작할 수 없다.

undefined  null => undefined 쓰지마세요. null 쓰기

변수에 값을 할당해주지 않았을때 undefined 상태이므로 비교할때 쓰자.
기본적으로 컴퓨터가 정해준 값 undefined
사용자가 변수값을 지웠을 경우 null로
즉 이 두개를 구분할 수 있다. undefined 쓸 수는 있어도 권장하는것은 null

prompt()를 이용하여 사용자의 입력을 받을 수 있다
입력받은 값은 문자형으로 입력받는다.
Number() 를 통해 숫자형으로 형변환 할 수 있다.!